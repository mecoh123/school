@model IEnumerable<School.Models.Course>
@{ 
    ViewData["Title"] = "Edit";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Edit</h2>

<ul class="list-group">
    @foreach (var item in Model)
    {
        <li>@Html.DisplayFor(model => item.Description)</li>
    }
</ul>
